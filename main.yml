---
# linux install jdk and httpd

- hosts: all
  gather_facts: true
  become: true
  roles:
# install jdk 
  - name: install jdk
    role: httpd
    tags:
      - httpd
# install httpd
  - name: install httpd services
    role: Java
    tags:
      - Java
